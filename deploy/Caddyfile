# The trusted production origin is the Tailscale Funnel hostname below.
# Keep the IP-derived hostname as an HTTP-only compatibility redirect so
# Caddy never attempts ACME issuance through a cloud firewall it cannot manage.
http://188.166.253.203.sslip.io {
	bind 188.166.253.203
	redir https://xauby-vps.tailfcdd3a.ts.net{uri} permanent
}
